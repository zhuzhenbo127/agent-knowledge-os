# Agent Knowledge OS：从 0 开始操作说明

这份说明写给第一次使用 Agent Knowledge OS 的普通用户。你不需要先学会知识管理，也不需要自己编写配置文件。大部分操作通过和 Agent 对话完成。

## 最终会得到什么

完成一次引导后，你会得到一套可以开始工作的 Markdown/Obsidian 知识库，包括：

- 清晰的中英文目录。
- 一份属于你的知识库章程。
- 原始素材入口和候选知识审核区。
- 概念、方法论、内容任务等模板。
- Obsidian 基础插件配置。
- ingest 处理日志、交付台账和知识缺口池。

这不是全自动知识库。AI 负责扫描、查重和草拟候选知识；你负责确认什么值得进入正式知识库。

## 最简单的安装方式

客户不需要打开 GitHub，也不需要自己输入终端命令。把下面这句话发给支持 Agent Skills 的 Agent：

> 安装并使用这个 Skill：https://github.com/zhuzhenbo127/agent-knowledge-os

Agent 会自动选择原生 Skill 安装器或通用安装器，完成安装和验证，然后开始首次引导。

部分 Agent 会在全局安装前要求确认一次权限。这是 Agent 自身的安全机制，不是 GitHub 登录，也不是双重验证。

如果 Agent 只总结仓库而没有安装，补发：

> 不要总结网页。请按照仓库根目录的 `AGENTS.md` 安装 `agent-knowledge-os`，验证后立即开始首次引导。

下面的工具准备和手工命令只用于排查安装问题，普通客户可以直接跳到“第 2 步”。

## 第 0 步：备用环境检查

你需要：

1. 一台安装了 Git 的电脑。
2. 一个支持安装 Agent Skills 的 Agent，例如 Codex、Claude Code、Cursor 或其他兼容客户端。
3. Python 3.10 或更高版本。
4. Obsidian，可选但推荐。如果未安装，Skill 会先询问你，获得确认后自动安装。

检查 Git 和 Python：

```bash
git --version
python3 --version
```

Windows 如果没有 `python3` 命令，可以尝试：

```powershell
python --version
```

## 第 1 步：手工安装备用方案

只有 Agent 无法自动安装时，才在终端运行：

```bash
npx skills add https://github.com/zhuzhenbo127/agent-knowledge-os --skill agent-knowledge-os
```

安装器会询问要安装到哪个 Agent。选择你平时使用的 Agent 即可。普通客户无需执行这一节。

如果希望在所有项目中使用，可以按安装器提示选择全局安装，或运行：

```bash
npx skills add https://github.com/zhuzhenbo127/agent-knowledge-os --skill agent-knowledge-os -g
```

检查是否安装成功：

```bash
npx skills list
```

如果你的 Agent 不支持 `npx skills`，可以手动安装：

```bash
git clone https://github.com/zhuzhenbo127/agent-knowledge-os.git
```

然后把仓库中的 `skills/agent-knowledge-os/` 复制到该 Agent 的 Skills 目录。

## 第 2 步：决定新建还是接管

只需要判断一件事：你现在有没有正在使用的 Markdown 或 Obsidian 知识库？

### 完全从零开始

先在电脑上选一个准备存放知识库的位置，然后在该目录打开 Agent，对它说：

> 使用 `agent-knowledge-os` 帮我从零搭建一个知识库。先完成引导，在我确认设置前不要写入文件。

### 已经有知识库

先备份现有知识库，再让 Agent 在知识库目录中工作，对它说：

> 使用 `agent-knowledge-os` 接管这个知识库。先只读扫描现状，不移动、不重命名现有文件，然后只询问无法自动发现的设置。

接管模式不会自动搬迁旧目录。需要调整目录时，Agent 只能先给迁移计划，等你另外确认。

## 第 3 步：完成八步引导

Agent 每次只会问一个问题。你需要依次确定：

1. **知识库身份**：叫什么、如何称呼你、主要服务什么。
2. **首要任务**：最多三个，例如内容创作、学习研究、工作决策。
3. **长期领域**：选择 3—7 个长期积累方向，不要把一次性项目当成领域。
4. **素材来源**：例如个人表达、文章书籍、用户问题、会议访谈。
5. **资产类型**：概念和方法论是基础，可增加案例、模板、表达规则、决策。
6. **复查周期**：哪些知识容易过期，多久复查一次。
7. **隐私边界**：哪些内容必须脱敏，哪些目录 AI 只能读不能写。
8. **Obsidian 设置**：先自动检测应用；未安装时询问是否授权自动安装，然后分别询问 Dataview 或 Templater。

不知道怎么回答时可以直接说：

> 我不确定，请先给我三个适合新用户的选择，并说明差异。

长期领域建议写成“中文名 + 简短英文名”，例如：

- 人工智能·AI
- 内容创作·Content
- 个人业务·Business

## 第 4 步：检查设置确认单

八步结束后，Agent 会先给你一份“个性化设置确认单”。重点检查：

- 知识库将创建在哪里。
- 名称、任务和长期领域是否正确。
- 哪些目录允许 AI 写入。
- 哪些内容需要脱敏。
- Obsidian 是否已安装；如需安装，将使用什么渠道和作用范围。
- 是否选择了社区插件。
- Agent 明确承诺不会移动、删除或覆盖什么。

需要修改时直接指出，不要先确认。例如：

> 把“短视频项目”从长期领域删除，改成“用户研究·Research”，然后重新给我确认单。

全部正确后再说：

> 确认以上设置，开始创建。完成后运行检查并告诉我下一步。

只有你明确确认后，Skill 才会生成正式配置。

## 第 5 步：检查生成结果

完成后，知识库至少应该包含：

```text
知识库/
├── 知识库说明·WIKI.md
├── AGENTS.md
├── CLAUDE.md
├── 00-收件箱·Inbox/
├── 01-原始素材·Sources/
├── 02-概念·Concepts/
├── 03-方法论·Methodologies/
├── 04-个人输出·Personal/
├── 05-工具集·Toolkit/
├── 90-知识地图·MOCs/
└── 99-系统·System/
```

先打开：

`00-收件箱·Inbox/从这里开始·START_HERE.md`

如果 Agent 报告 lint error，不要继续 ingest。让它先解释问题并修复；warning 通常表示需要人工判断，不代表知识库损坏。

## 第 6 步：在 Obsidian 中打开

如果使用 Obsidian：

1. 打开 Obsidian。如果你在确认单中授权了自动安装，Agent 会先完成安装并报告安装位置。
2. 选择“打开本地仓库”或“Open folder as vault”。
3. 选择刚创建的知识库根目录。
4. 检查左侧文件列表是否显示全部中英文目录。

官方核心插件会通过配置启用，并保留你原有的插件设置。

Dataview 和 Templater 属于社区插件。只有你在确认单中明确选择，并且电脑已启用 Obsidian 1.12.7+ 官方 CLI 时，Skill 才会尝试安装。没有安装成功也不影响基础知识库使用。

## 第 7 步：放入第一份原始素材

V1 只直接处理 Markdown 文件。先选择一份真实、正文完整的素材，例如：

- 你自己写过的一篇文章。
- 一次访谈或会议的完整文字稿。
- 一次真实项目复盘。
- 一组用户问题及你的回答。

把文件复制到：

`01-原始素材·Sources/对应来源目录/`

例如：

`01-原始素材·Sources/个人表达·Personal/我对个人知识库的理解.md`

注意：

- 文件中必须有真实正文，不能只有标题或链接。
- 不要把密码、密钥、账号或未脱敏客户资料放进去。
- Skill 不会在 V1 中自动抓网页、OCR 图片或转录 PDF。
- 放入 Sources 后，不要让 AI 改写这份原文。

## 第 8 步：运行第一次 ingest

对 Agent 说：

> 使用 `agent-knowledge-os` 扫描新增素材。这次只处理一份，全文读取，先告诉我处理结果，不要修改正式知识。

每份素材只会得到四种结果之一：

| 结果 | 含义 |
|---|---|
| 跳过·Skip | 无有效正文、无关或无法安全处理 |
| 保留原文·Preserve | 有参考价值，但没有独立的新知识 |
| 更新建议·Update | 可以补充已有知识，但不直接修改正式知识 |
| 候选草稿·Candidate | 出现值得独立审核的新判断 |

`零条候选`也是正常结果。不要要求 AI 为了数量强行生成知识卡片。

如果产生候选，它只能出现在：

`00-收件箱·Inbox/`

## 第 9 步：人工审核候选知识

打开候选文件，重点检查：

1. 核心判断是不是来源真实表达的内容。
2. 来源路径和来源哈希是否存在。
3. 适用场景和不适用边界是否清楚。
4. 是否只是重复已有知识。
5. 是否包含需要脱敏的信息。
6. 应该新建、合并、补充来源还是拒绝。

然后对 Agent 说：

> 使用 `agent-knowledge-os` 和我逐条审核 Inbox 中的候选。每次只处理一条，先给出来源证据和建议，未经我确认不要晋升。

你可以使用这些明确决定：

- “确认晋升为概念。”
- “合并到某条已有知识，只补充来源。”
- “保留候选，等待第二个来源。”
- “拒绝晋升，记录原因。”

正式知识的晋升始终由你决定。

## 第 10 步：用知识库完成第一次内容输出

不要直接说“根据知识库写一篇文章”。先让 Agent 帮你定义任务：

> 使用 `agent-knowledge-os` 帮我完成一次内容输出。先依次问清写给谁、对方的问题、核心判断、不能说什么、使用哪些证据、输出形式。先给素材包，不要直接写初稿。

正确顺序是：

1. 你确认内容任务。
2. Agent 返回带来源的素材包。
3. 你选择采用哪些素材。
4. Agent 给出内容结构。
5. 你确认结构。
6. Agent 才生成初稿。
7. 你完成最终判断和修改。

知识库是证据库，不是作者；AI 是执行助手，不是主编。

## 第 11 步：把真实反馈写回来

内容发布、项目交付或决策完成后，对 Agent 说：

> 使用 `agent-knowledge-os` 记录这次真实交付。先整理交付物、使用的知识、新判断、反馈数据、暴露缺口和待复查项，给我确认后再回写。

Skill 会帮助更新：

- `99-系统·System/交付台账·DELIVERY_LEDGER.md`
- `99-系统·System/知识缺口池·KNOWLEDGE_GAPS.md`

一次成功经验仍然先进入候选区，不会自动成为正式方法论。

## 第 12 步：建立最小日常习惯

不需要每天整理全部资料。建议从这个节奏开始：

### 随时

- 把正文完整的素材放入 Sources。
- 不急着分类到 Concepts 或 Methodologies。

### 每周一次

对 Agent 说：

> 使用 `agent-knowledge-os` 检查本周新增素材和 Inbox，只给我本周需要处理的 Todo List。

### 每月一次

对 Agent 说：

> 使用 `agent-knowledge-os` 对知识库运行 lint，列出过期、待复查、来源变化、断链和孤立知识，不要自动删除或修改。

## 重新进行个性化设置

需要改变任务、领域、复查或隐私设置时，对 Agent 说：

> 使用 `agent-knowledge-os` 重新进行知识库个性化。先显示当前配置，只询问我要改的部分，先给修改前后对照，不要直接改目录。

Agent 会先生成差异。确认后才更新配置和章程；涉及目录变化时只生成迁移计划，不会自动重命名已有目录。

## 常见问题

### Agent 没有调用 Skill

使用完整名称：

> 使用 `agent-knowledge-os` ……

并用 `npx skills list` 检查是否安装到当前 Agent。

### 安装后找不到 Skill

重新运行安装命令，确认选择了正在使用的 Agent。部分客户端需要重启或开启一个新会话后才能发现新 Skill。

### 为什么确认前没有生成目录

这是安全设计。首次引导结束前，Agent 只能给确认单；只有你明确说“确认创建”后才写入。

### 为什么候选没有自动进入 Concepts

这是系统铁律。AI 生成的是待审候选，不是已经成立的正式知识。

### 为什么 PDF、网页或图片没有自动处理

V1 不包含自动抓网页、OCR 和 PDF 转录。请先用可信工具转换成正文完整的 Markdown，并保留原始来源信息。

### 为什么 Dataview 或 Templater 没有安装

社区插件必须明确授权，并依赖 Obsidian 官方 CLI。基础知识库不依赖这两个插件，可以先继续使用。

### 为什么安装 Obsidian 时还有一次系统确认

你在引导确认单中授权的是安装意图；操作系统或 Agent 仍可能对下载、全局安装或执行安装器弹出安全确认。这不是 GitHub 登录，Skill 也不会绕过该机制。

### 已有知识库能不能直接套用新目录

可以接管，但不会自动搬迁。先让 Agent 扫描和生成迁移计划，再逐步确认。

### 可以让 AI 全自动晋升知识吗

不建议，也不在本 Skill 的安全边界内。自动晋升会把误读、重复和过期判断直接写进正式知识库。

## 最小验收清单

完成以下项目，就说明你的第一版知识操作系统已经开始运行：

- [ ] Skill 已安装并能被当前 Agent 调用。
- [ ] 八步引导完成，并确认了个性化设置。
- [ ] 固定目录、配置、章程和起步页已经生成。
- [ ] Obsidian 能正常打开知识库。
- [ ] Sources 中至少有一份正文完整的 Markdown 素材。
- [ ] 已完成一次 ingest，并看到四类结果之一。
- [ ] 已人工审核至少一条候选，或确认本次没有净新知识。
- [ ] 已完成一次“任务定义 → 素材包 → 结构 → 初稿”的输出。
- [ ] 已记录一次真实交付或一个知识缺口。
- [ ] lint 没有未处理的 error。

做到这里，不代表知识库已经“建完”，而是说明它已经形成第一个可持续循环。
